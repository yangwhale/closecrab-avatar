# 控制面（CPU 机器，无 GPU 依赖）
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
PyJWT>=2.9
livekit-api>=0.8          # 只用 AccessToken 铸票

# worker 额外需要（GPU 机器）
# livekit-agents>=1.5
# livekit>=0.17
# numpy, pillow

# 冒烟测试用（scripts/install.sh 不装，只有跑 smoke.py 时才需要）
pillow>=10.0          # 只为量参考图的宽高；没有它也能跑，宽高显示 0
httpx>=0.27
